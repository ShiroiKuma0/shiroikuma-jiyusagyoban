# Android / Gradle
*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
app/build/
fdroid/build/
fdroid/logs/
fdroid/repo/
fdroid/srclibs/
fdroid/tmp/
fdroid/unsigned/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.ap_
*.dex
*.class
release/

# Keystores (NEVER commit)
*.jks
*.keystore
keystore.properties

# Kotlin
.kotlin/

# Local agent working notes
CODEX_CHANGELOG.md
*.bak
AUDIT_COMPLETE.md
PHASE_5_CHECKPOINT.md
build.log
buildlog.txt
build_output.txt
build_*.bat
build.sh

# shiroikuma fork: CLAUDE.md, .claude/skills, .md docs and hand-off.md ARE committed;
# only host-specific local Claude settings stay out.
.claude/settings.local.json

# Claude's scratch space: generator/demo/sim scripts and their JSON output live here
# (never in ~/tmp — that's 白い熊's). Pushed to the device's /sdcard/tmp/ from here.
.scratch/
