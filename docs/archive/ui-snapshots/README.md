# Archived UI Snapshots

This folder contains pre-reintegration Compose screen snapshots that were previously kept as `.kt.bak` files under `app/src/main/java/com/opentasker/ui/screens`.

They are retained as historical reference only. Active application code should live under `app/src/main/java`; do not import from this archive.
