<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission
        android:name="${smsPermissionName}" />
    <uses-permission
        android:name="${phoneStatePermissionName}" />
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />

    <permission
        android:name="com.opentasker.permission.AUTOMATION"
        android:description="@string/permission_automation_description"
        android:label="@string/permission_automation_label"
        android:protectionLevel="signature" />

    <queries>
        <!-- Sister app: token-gated automation intents driven by the Send Intent action. -->
        <package android:name="shiroikuma.jami" />
        <!--
          Broad visibility for the generic Send Intent action. Package visibility is
          all-or-nothing per package, so matching any launchable app's MAIN/LAUNCHER
          entry exposes every component of that package — letting Send Intent dispatch
          to any normal app's activities/services/receivers on Android 11+. Targets
          with no launcher entry, or reachable only by a custom action string, still
          need their own <package> pin (e.g. shiroikuma.jami above).
        -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <package android:name="com.termux" />
        <package android:name="com.termux.tasker" />
        <package android:name="moe.shizuku.privileged.api" />
        <intent>
            <action android:name="com.twofortyfouram.locale.intent.action.EDIT_SETTING" />
        </intent>
        <intent>
            <action android:name="com.twofortyfouram.locale.intent.action.EDIT_CONDITION" />
        </intent>
        <intent>
            <action android:name="com.twofortyfouram.locale.intent.action.FIRE_SETTING" />
        </intent>
        <intent>
            <action android:name="com.twofortyfouram.locale.intent.action.QUERY_CONDITION" />
        </intent>
        <intent>
            <action android:name="com.twofortyfouram.locale.intent.action.REQUEST_QUERY" />
        </intent>
    </queries>

    <application
        android:name=".OpenTaskerApp_NoHilt"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.OpenTasker"
        android:supportsRtl="true"
        android:allowBackup="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        tools:targetApi="s">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|density|keyboardHidden"
            android:theme="@style/Theme.OpenTasker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.nfc.action.TAG_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <service
            android:name="com.opentasker.core.engine.AutomationService"
            android:exported="false"
            android:foregroundServiceType="specialUse|location">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="白い熊 自由作業盤 automation engine — runs user-defined profiles when their contexts match" />
        </service>

        <service
            android:name="com.opentasker.core.contexts.NotificationTriggerService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <receiver
            android:name="com.opentasker.core.engine.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.opentasker.automation.receiver.TimeEventReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.opentasker.automation.TIME_TICK" />
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <service
            android:name="com.opentasker.core.scenes.SceneOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Scene overlay — displays user-defined floating UI panels" />
        </service>

        <service
            android:name="com.opentasker.core.contexts.QuickSettingsTileService"
            android:exported="true"
            android:icon="@android:drawable/ic_menu_compass"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <activity
            android:name="com.opentasker.widget.TaskRunActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity="" />

        <activity
            android:name="com.opentasker.scenes.SceneActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <activity
            android:name="com.opentasker.scenes.WakedanceActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <activity
            android:name="com.opentasker.widget.TaskWidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.OpenTasker">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <receiver
            android:name="com.opentasker.widget.TaskWidgetProvider"
            android:exported="false"
            android:label="@string/widget_task_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_task_info" />
        </receiver>

        <receiver
            android:name="com.opentasker.widget.StyledWidgetProvider"
            android:exported="false"
            android:label="@string/widget_styled_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/styled_widget_info" />
        </receiver>

        <activity
            android:name="com.opentasker.widget.StyledWidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.OpenTasker">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- External "name this widget" bridge for sister apps (e.g. the raikidoban launcher). Explicit
             ordered broadcast, no permission: senders may use a different signing key. -->
        <receiver
            android:name="com.opentasker.widget.SetWidgetNameReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="shiroikuma.jiyusagyoban.action.SET_WIDGET_NAME" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.opentasker.core.actions.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.opentasker.action.NOTIFICATION_BUTTON" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.opentasker.core.external.AutomationTargetReceiver"
            android:exported="true"
            android:permission="com.opentasker.permission.AUTOMATION">
            <intent-filter>
                <action android:name="com.opentasker.action.RUN_TASK" />
                <action android:name="com.opentasker.action.SET_PROFILE_ENABLED" />
                <action android:name="com.opentasker.action.QUERY_STATUS" />
            </intent-filter>
        </receiver>

        <activity
            android:name="com.opentasker.core.plugins.locale.LocaleSettingEditActivity"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="OpenTasker task"
            android:theme="@style/Theme.OpenTasker">
            <intent-filter>
                <action android:name="com.twofortyfouram.locale.intent.action.EDIT_SETTING" />
            </intent-filter>
        </activity>

        <receiver
            android:name="com.opentasker.core.plugins.locale.LocaleSettingFireReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.twofortyfouram.locale.intent.action.FIRE_SETTING" />
            </intent-filter>
        </receiver>

        <activity
            android:name="com.opentasker.core.dialog.DialogActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <service
            android:name="com.opentasker.core.accessibility.ShiroiKumaAccessibilityService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>
</manifest>
